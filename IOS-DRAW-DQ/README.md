# Project Created in Mobile App Development, Fall 2023
## IOS APP create in Xcode in Swift with UIKit
  * Objective was to create a dymanic object using `func draw(_ rect: CGRect)` and ` DispatchQueue.main.asyncAfter()`
